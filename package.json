{
  "name": "mailfalcon",
  "version": "0.0.0",
  "private": true,
  "description": "Freemium Gmail email tracker (opens, clicks, real-time notifications).",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "gen:icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "sharp": "^0.35.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=22"
  }
}
